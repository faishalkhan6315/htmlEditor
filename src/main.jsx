import React from 'react'
import ReactDOM from 'react-dom/client'
import VisualHtmlEditor from './components/VisualHtmlEditor.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <VisualHtmlEditor />
  </React.StrictMode>,
)
